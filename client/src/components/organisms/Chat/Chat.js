import React from "react";
import styled from "styled-components";

const Wrapper = styled.div`
  position: relative;
  left: -60%;
`;

const Chat = () => (
  <Wrapper>
    <h1>Hello world!</h1>
  </Wrapper>
);

export default Chat;
